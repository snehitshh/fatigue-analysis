{
    "name": "fatigue-analysis",
    "version": "2.0.0",
    "private": true,
    "type": "module",
    "description": "Browser-based fatigue analysis experiment with Supabase backend scaffolding.",
    "scripts": {
        "dev": "vite --host 127.0.0.1",
        "build": "vite build",
        "build:app": "vite build && node scripts/build-app.mjs",
        "preview": "vite preview --host 127.0.0.1",
        "test": "node --test"
    },
    "dependencies": {
        "@capacitor/android": "^6.2.1",
        "@capacitor/cli": "^6.2.1",
        "@capacitor/core": "^6.2.1",
        "@mediapipe/tasks-vision": "^0.10.20",
        "@supabase/supabase-js": "^2.50.0"
    },
    "devDependencies": {
        "vite": "^8.0.16"
    }
}
